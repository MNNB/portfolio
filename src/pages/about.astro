---
import Layout from '../layouts/Layout.astro';
---

<Layout 
  title="About | F-XD"
  description="Product Designer with a focus on creating intuitive digital experiences through thoughtful UX/UI design."
>
  <section class="about">
    <div class="container">
      <div class="about-content">
        <div class="about-text">
          <h1 class="title">About</h1>
          <div class="bio">
            <p>I'm a business-minded designer currently focusing on HTML, CSS and JavaScript to create better digital products. With experience working across different cultures and markets, I bring a global perspective to design challenges.</p>
            
            <div class="stats">
              <div class="stat-item">
                <span class="number">10</span>
                <span class="label">Years Experience</span>
              </div>
              <div class="stat-item">
                <span class="number">3</span>
                <span class="label">Countries Worked</span>
              </div>
            </div>

            <div class="skills">
              <h2>Skills</h2>
              <ul class="skill-list">
                <li>UX Design</li>
                <li>UI Design</li>
                <li>Web Design</li>
                <li>Product Design</li>
                <li>Design Systems</li>
                <li>Frontend Development</li>
              </ul>
            </div>
          </div>
        </div>
        <div class="about-image">
          <img 
            src="/images/me.png" 
            alt="Portrait photo" 
            width="600" 
            height="800"
          />
        </div>
      </div>
    </div>
  </section>
</Layout>

<style lang="scss">
  .about {
    min-height: 100vh;
    padding: calc(var(--spacing-xl) * 2) 0;
    display: flex;
    align-items: center;
  }

  .container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 var(--spacing-md);
  }

  .about-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--spacing-xl);
    align-items: center;
  }

  .title {
    font-size: var(--font-size-4xl);
    margin-bottom: var(--spacing-lg);
    text-transform: uppercase;
    letter-spacing: -0.02em;
  }

  .bio {
    p {
      font-size: var(--font-size-xl);
      color: var(--color-muted);
      margin-bottom: var(--spacing-lg);
      max-width: 45ch;
    }
  }

  .stats {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--spacing-md);
    margin-bottom: var(--spacing-xl);
  }

  .stat-item {
    .number {
      display: block;
      font-size: var(--font-size-3xl);
      line-height: 1;
      margin-bottom: var(--spacing-xs);
    }

    .label {
      font-family: var(--font-mono);
      font-size: var(--font-size-sm);
      color: var(--color-muted);
    }
  }

  .skills {
    h2 {
      font-size: var(--font-size-xl);
      margin-bottom: var(--spacing-md);
      text-transform: uppercase;
      letter-spacing: 0.1em;
    }
  }

  .skill-list {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--spacing-sm);
    list-style: none;
    padding: 0;

    li {
      font-family: var(--font-mono);
      font-size: var(--font-size-base);
      color: var(--color-muted);
      padding: var(--spacing-xs) 0;
      border-bottom: 1px solid var(--color-border);
    }
  }

  .about-image {
    position: relative;
    
    img {
      width: 100%;
      height: auto;
      object-fit: cover;
    }

    &::after {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: var(--color-accent);
      mix-blend-mode: multiply;
      opacity: 0.1;
    }
  }

  @media (max-width: 768px) {
    .about-content {
      grid-template-columns: 1fr;
      gap: var(--spacing-xl);
    }

    .about-image {
      order: -1;
    }

    .stats {
      margin-bottom: var(--spacing-lg);
    }
  }
</style> 